@model List<GitCommit>

<h2>Commits</h2>
<ul>
@foreach (var commit in Model)
{
    <li>
        <strong>@commit.Sha.Substring(0, 7)</strong> - @commit.Message
        <br />
        <em>@commit.Author</em> on @commit.Date
    </li>
}
</ul>
